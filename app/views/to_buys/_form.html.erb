<%= simple_form_for(@to_buy) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :user %>
    <%= f.association :dish %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
