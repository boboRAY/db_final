<%= simple_form_for @order, :html => { :class => 'form-horizontal form-model' } do |f| %>
  <div class="form-group">
    <div class="col-md-10">
      <% res = Restaurant.find(params[:rid]) %>
      你現在在訂的餐廳是 <%= res.name %>, 他有如下的折價卷：<br>
      <%= f.association :discount, collection: res.discounts %>

      <%= f.association :dishes, collection: res.dishes %>
      <%= f.association :location %>
    </div>
  </div>

  <div class="form-group">
    <div class='col-md-offset-2 col-md-10'>
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                  orders_path, :class => 'btn btn-default' %>
    </div>
  </div>
<% end %>
