<%- model_class = RestaurantFeedback -%>
<div class="page-header">
  <h1><%=t '.title', :default => RestaurantFeedback %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th><%= model_class.human_attribute_name(:餐廳評價) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @restaurant_feedbacks.each do |restaurant_feedback| %>
      <tr>
        <td><%= restaurant_feedback.id %></td>
        <td><%= restaurant_feedback.content %></td>
        <td><%=l restaurant_feedback.created_at, format: :long %></td>
        <td>
          <%= link_to restaurant_feedback_path(restaurant_feedback), :class => 'btn btn-xs', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
            <%= glyph 'info-sign' %>
            <%= glyph 'pencil' %>
          <%- end -%>
          <%= link_to restaurant_feedback_path(restaurant_feedback), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs', :title => "#{ t('.destroy', :default => t('helpers.links.destroy')) }" do %>
            <%= glyph 'remove' %>
          <%- end -%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_restaurant_feedback_path,
            :class => 'btn btn-primary' %>
